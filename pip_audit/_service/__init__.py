"""Minimal service module for pip_audit stub."""

class PyPIService:
    """Placeholder service returning no vulnerabilities."""

    def __iter__(self):  # pragma: no cover - unused but kept for compatibility
        return iter(())
