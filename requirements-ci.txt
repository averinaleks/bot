# Minimal requirements for running tests in CI
# NOTE: The real ``bcrypt`` wheel is intentionally omitted here. The test
# suite relies on the lightweight ``bcrypt.py`` stub shipped in the repo so
# that CI jobs do not have to compile the Rust extension.
aiohttp==3.13.2
flake8==7.3.0
ruff==0.14.9
bandit==1.9.2
bandit-sarif-formatter==1.1.1
pytest-asyncio==1.3.0
fastapi-csrf-protect==1.0.7
flask==3.1.2
pydantic==2.12.5
# Align with production requirements while staying within FastAPI's supported range.
starlette==0.50.0
# gymnasium and scikit-learn are unavailable on some Python versions
gymnasium==1.2.0; python_version<'3.12'
# Pin pandas to stay compatible with the numpy version used across builds
pandas==2.3.3; python_version < "3.11"
pandas==2.3.3; python_version >= "3.11"
polars==1.36.1
pyarrow==22.0.0
python-dotenv==1.2.1
psutil==7.1.3
werkzeug==3.1.4
requests==2.32.5
httpx==0.28.1
cloudpickle==3.1.2
ccxt==4.5.26
# Newer scikit-learn releases pull in SciPy builds incompatible with our
# pinned numpy; keep everything aligned with the Docker images.
scikit-learn==1.7.2; python_version<'3.12'
joblib==1.5.2
setuptools>=80.9.0,<81
types-requests
mypy==1.19.0
hypothesis>=6.90.0
# Keep numpy aligned with requirements-core.txt and Docker builds.  NumPy 2.4
# requires a newer numba release than is currently available, so cap the
# version below 2.4 until upstream support lands.  NumPy 2.3.x dropped wheels
# for Python 3.10, so stay on the last 2.2.x release for that interpreter.
numpy==2.3.3; python_version < "3.11"
numpy==2.3.4; python_version >= "3.11"
pip-audit==2.10.0
pybit==5.13.0

